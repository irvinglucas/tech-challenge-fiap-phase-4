<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.fiap.feedback</groupId>
        <artifactId>tech-challenge-fiap-phase-4</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>weekly-report</artifactId>
    <name>weekly-report</name>
    <description>
        Cloud Function Gen2 (HTTP trigger). Thin inbound adapter invoked
        by Cloud Scheduler; delegates to GenerateWeeklyReport use case.
    </description>

    <dependencies>
        <dependency>
            <groupId>com.fiap.feedback</groupId>
            <artifactId>common</artifactId>
        </dependency>

        <!-- Quarkus core extension for implementing HttpFunction directly. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-google-cloud-functions</artifactId>
        </dependency>

        <!-- JSON serialization for the response body -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jackson</artifactId>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-logging-json</artifactId>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
