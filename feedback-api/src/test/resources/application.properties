# Test-only overrides. Production beans are replaced with @Mock alternatives,
# so the values below only need to be non-null/parseable to keep Quarkus happy
# during startup.
quarkus.google.cloud.project-id=test-project
feedback.admin-email=admin@example.com
quarkus.mailer.mock=true
quarkus.log.console.json=false
